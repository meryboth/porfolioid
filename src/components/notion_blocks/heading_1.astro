---
import type { NotionBlock } from "../../lib/types";
import BlogRender from "../blog_render.astro";

interface Props {
  block: NotionBlock;
}

let { block } = Astro.props;
---

<h1 set:html={block.text} class="pt-[30px] pb-0" />
{block.children_level != null ? <BlogRender blocks={block.children} /> : ""}

<style>
  h1 {
    font-weight: 400;
    font-size: 3rem;
  }
</style>
